# Threat intelligence.
#
# Shodan and GreyNoise are queried without any key at all, so enrichment works
# on a fresh clone. These two keys add the reputation sources on top. Both are
# free to create and the same key works across projects.
#
#   https://www.virustotal.com/gui/my-apikey
#   https://www.abuseipdb.com/account/api
VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=
